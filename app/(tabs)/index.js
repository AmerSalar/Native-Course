import { Text, View } from "react-native";

export default function App() {
  return (
    <View className={`flex-1 justify-center items-center bg-background`}>
      <Text className={`text-primary font-sans-regular`}>Hello World</Text>
    </View>
  );
}
